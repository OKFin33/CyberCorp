# Corp entry

Read the project direction at `{{PROJECT_REF}}` from the repository root. [canon-map.yaml](canon-map.yaml) locates existing fact owners and the current native delivery focus. Follow root and target-path instructions; product meaning remains with accepted product decisions, not incidental implementation drift.

## Enter and continue

{{SHARED_WORK}}

If the user requested local creation only, complete local adaptation and verification, and leave the missing shared-work setup and resume condition here. Do not expand that request into remote setup. This entry is the handoff location; generic method files do not need copies of the same temporary status.

For an existing clone, [bootstrap.md](bootstrap.md) explains how to obtain a fixed remote version in an independent checkout while preserving the original work. Read that checkout's rules in the actual worker runtime. Getting newer rule files does not automatically change a task's accepted inputs or prove the runtime reloaded its instructions.

For current work, use [development-loop.md](development-loop.md). It routes discovery, execution, review and recovery. Read only the method needed for the actual work; there is no required sequence through every method.

When establishing or completing project preparation, read `.agents/skills/prepare-corp/SKILL.md` from the repo root. Whole-project preparation is an evidence-based handoff, not a count of generated documents. Installation itself does not establish shared work or complete either phase.

## Supplied material routes

{{SOURCES}}

Read relevant inputs and reconcile them with their actual owners. Source material can remain a reference; preserve the distinction between received, adopted, implemented and verified. Inputs that another Corpo truly needs must be accessible through the project's shared surfaces.

## Project ownership

Git owns versioned content; native Milestones and Issues own goals/work; comments own execution and recovery records; PRs and CI own their corresponding evidence. `.agents/corp/install.json` records initial file provenance only. Do not maintain another task ledger here.

The creator and external launch tools can leave. A new authorized Corpo still needs a runtime, ordinary dependencies and access to the repository. No process is automatically started by these files.
