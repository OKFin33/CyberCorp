候选在导入时按文件字节的 SHA-256 判重，保留首份；同名但字节不同的文件仍分别导入。实现流式读取所有待导入文件，取舍是增加磁盘读取，以避免依赖文件名和修改时间判重。现有导入不变，也不清理存量库中的重复文件；旧版重复行为尚未测量。

固定候选范围为 base `1111111111111111111111111111111111111111` → head `2222222222222222222222222222222222222222`。给定的合成证据记录：导入 120 个唯一文件、跳过 30 个字节相同的副本；2 个不可读文件使运行失败并报告路径，没有宣称完整导入。样本峰值内存 12 MB，仅适用于该 fixture，不能外推真实档案或慢盘表现。材料报告固定 head 上现有 9 项测试通过，复现命令为 `python3 -m unittest discover -s tests`；本次只读取证据包，未运行测试。真实档案、慢盘运行、托管 CI 和独立审阅均未完成。

建议将查重纳入下一轮试点，以真实档案补查正确性和额外 I/O 的耗时；目前没有证据支持增加哈希缓存的复杂度。纳入意味着试点承担完整读取带来的开销，并验证上述缺口；暂缓则下一轮不包含这项变化。请决定是否纳入下一轮试点。当前请求是产品范围决定，材料尚未提供可供授权的合并 PR、检查和方法，因此不请求代码合并授权。

精确依据：[candidate.md](../../../../tests/scenarios/communication/facts.md)，以及材料列出的 [PR #9（虚构证据入口）](https://github.com/fixture/archive/pull/9)。
