# 变更记录

本文件记录已交付的变化。进行中的工作由同仓 [Issues](https://github.com/OKFin33/CyberCorp/issues) 与 [Milestone](https://github.com/OKFin33/CyberCorp/milestone/1) 持有，能力的验证程度见 [验证状态](docs/verification-status.md)，**每项变化背后的理由与被否决的替代方案见 [决策记录](docs/decisions/)**。

版本号形如 `0.1.0a5` 的是创建包候选，不代表默认分支已采用或行为已验收。

## 0.1.0a10

**未满足的前置条件进入观察结果。** 取用工作要求前置已满足，而全局观察此前不报告任何前置信息——规则要求执行者判断的东西，数据里没有。于是一个声明了未关闭依赖的 Issue 会被报成可取。

- 每条 Issue 增加 `open_prerequisites`，只列仍未关闭的前置编号。空列表意味着这一项不阻碍取用。
- 依赖摘要随列表读取一并返回，因此判断"有无未满足前置"零额外请求；只有确实存在未满足前置的 Issue 才多读一次明细。
- 机制层的可取性信号由两个变为三个，并说明三者不可互换：无候选、无 Owner 决定标签的 Issue，只要它声明的真实依赖仍未关闭，就仍然不可取。
- `work` 的选择规则改为直接引用观察结果报告的三项，而不是要求执行者自行确认前置。

**破坏性**：无。新增字段，旧 payload 缺少依赖摘要时报告为空列表。

**候选与提及不再是同一件事。** 观察此前把候选 PR 描述里出现的每个 Issue 编号都当作"这个 PR 是它的候选"，于是在描述里引用一个 Issue 作为上下文，会让那个 Issue 显示为已被占用——**可取的工作看起来不可取**。

- PR 摘要区分两个字段：`mentions_issues` 是描述与标题里出现的全部编号（取用前值得看一眼），`delivers_issues` 是它真正作为候选的那些。
- 候选身份由两个信号中的任一个成立：描述里的关闭关键词（`closes`／`fixes`／`resolves`），或分支名里带 Issue 编号。两者都随已发生的读取返回，不增加请求；任一独立成立，因此描述措辞不同的项目仍能靠分支识别，分支自由命名的项目仍能靠关键词识别。
- 可取性只看 `delivers_issues`。

## 0.1.0a9 — work 的身份是实现者

**取代 `0.1.0a8`；尚未采纳 `a8` 的项目直接采纳本版。** 两版之间只有方法文件的一处实质变化，无需分两轮跟进。

- **`work` 的产出定义为"能跑且检查通过的变更"。** 此前它的 description 提供 `implement, investigate or resume` 三种形态，而调查是更便宜的那个且总有产出——把它列为正当形态，就把最常走的那条路偏向了产出说明而非产出变更。另两类由触发条件保护（审查只在关闭条件成立时开启、有界统筹只在无可取单元时触发），`work` 门槛最低而没有这一层。**过程不受约束**：文件明说阅读、搜索、试做、丢掉第一版都是执行者的选择，受约束的只有交付物。
- **四份方法文件的交接闭合。** `work`、`intake`、`review` 都把发现交给有界统筹，而它此前的读取清单里没有这三个来源——三份文件指着一个不读的读者。
- **`work` 不再复述机制层的完成定义**，改为引用；**`review` 补齐边界节与故障清单**，四份结构一致。

理由与被删去的解释（按"无法论证必须存在即不存在"逐条检验，仅两句留下）见[决策记录 0014](docs/decisions/0014-work-is-implementation.md)。

## 0.1.0a8 — 四类工作、状态路由与前提声明

**对采纳者的影响较大，逐项列出。** 方法文件从三份变为四份且全部改名：`prepare` 移出运行侧（成为创建包的 `establish.md`），新增 `intake` 与 `bounded-planning`，`work-corp`／`review-corp` 去掉后缀。入口结构重构，必读区由 9206 字符降至 3865。`canon-map` 可加可选的 `answers` 字段。`repo-context.py` 为每个 Issue 附带 `open_candidates`。`check.py` 对未完成建立的项目发提示。**取消「优先级最高」这个概念**——改为取任一前置满足的 Issue。

已运行 `0.1.0a7` 的项目升级时取新版机制层与四份方法文件，并按新入口结构重写自己的入口项目段落；`prepare` 的引用改指创建包。


- **四类工作各有方法文件，`-corp` 后缀去掉（破坏性）。** 新增 `bounded-planning`（有界统筹），`prepare-corp`／`work-corp`／`review-corp` 改名为 `prepare`／`work`／`review`。此前机制层有 planning 的规则而无方法文件，`prepare` 的一次性初始化定位占了常态规划的位置。已采纳的项目引用旧路径，需随本版更新。
- **入口首段改为按可读状态路由**，位于统领规则之前：跑一次 `repo-context.py`，按输出选工作类型。此前一次真实规划执行为决定做什么而读了 `mechanics.md` 全文 25188 字符——入口没给判定表，它只能从规则里推。
- **审查不创建工作**：阻塞项回原 Issue，缺少的单元交有界统筹并**由它更新关闭条件**，Owner 的决定交 Owner。漏掉更新会让旧关闭条件保持可满足，审查在已变化的阶段上反复开启。
- **Spec wave 从原生依赖关系派生，不存储批次列表**——存一份就是第二个权威来源，且随任一 Issue 移动而过期。
- 机制层补上：**open 不等于可取**（已交付待授权的工作应标为需要 Owner 决定）、有界统筹的三条边界及其理由、以及两条相应的故障形态。

- **取消「优先级最高」这个概念。** 改为：取任一现在可做的 Issue——在当前 Milestone、未标记需要 Owner 决定、且原生前置全部满足；多个符合就取任一个。排序是一种没有载体的状态，会随实例消失；而 Issue 在当前 Milestone 里已经意味着本阶段要交付它，先取哪个不改变阶段能否完成。真正的顺序约束本就该表达为依赖关系。**吞吐由多派实例解决，不由排对顺序解决。**

理由、被 Owner 当场纠正的一处顺序错误、以及取消优先级的完整推导见[决策记录 0011](docs/decisions/0011-four-work-types-and-state-routing.md)。

- **建立与运行分开（破坏性）。** `prepare` 移出 `.agents/skills/`，成为分发包的 `references/establish.md`（Phase 2，Owner 在场一次性完成）。分发侧从此三件齐全：装、配、换代。此前它混了配置与规划，后者与有界统筹重复，导致每个执行者每次开工都要判断一次"我是不是该先 prepare"。
- **新增 `intake`（第四类工作）。** Canon 的活性主要来自外部输入——需求变化、技术选型更换、新材料进来——而不是三类工作的副产品。机制层早有这条规则，缺方法文件（与 planning 同一形状的第二次出现）。它排在四类之首：已变更的前提会让后面三类全在错的基础上工作。
- **材料不自动进入仓库。** 进入的是提取出的不可推导事实加一个可得来源的引用，来源可以是"无"但不能省略。这条同时防 Canon 退化成材料堆、以及不该进来的东西被顺手存进来。
- **`canon-map` 每条可加 `answers` 字段**，说明它回答什么问题，使执行者不必打开数条来找需要的那条。字段可选，既有路由表继续有效。
- **入口重构为三段漏斗，必读区由 9206 字符降至 3865。** 你该做什么（六行状态表）→ 开工前的占用 → 统领规则与本项目清单；其余移到分隔线后并标明命中才读。信息一件未丢，只是从提前读变为需要时读。
- **检查点与释放合并为一条评论。** 它们是同一动作的两面，分成两条多一次往返而不多一个事实。
- **刚装完但未完成建立的项目会被明确提示**，而不是静默 `passed`——此前一个只做了一半的 Corp 能通过它自己的检查。

理由、四处被否决的替代方案（把 Canon 做成文档仓库、限制 intake 只能由人调用、等）见[决策记录 0012](docs/decisions/0012-establish-versus-run.md)。

- **声明前提：目标必须是已建立的 GitHub 托管仓库。** 机制的每一个载体都是 GitHub 原生对象，没有托管仓库不是降级可用而是载体不存在。声明放在 `README.md` 与创建包 `SKILL.md`——**正常工作的 Corpo 完全读不到它**，前提不满足时的结论只出现在工具的错误输出里，并给工作者与建立者各一句可执行动作。
- **区分「不是 Corp」与「暂时连不上」。** 新增 `Unreachable` 类别：连不上不能证明该仓库不是 Corp，只证明这次读取失败——暂停依赖它的决定，无关工作继续。此前两者都输出原始 `gh` 报错，执行者从中既读不出该停也读不出该继续。
- **`repo-context.py` 为每个 Issue 附带 `open_candidates`**（引用它的未合并 PR，零额外 API 调用）。**一个有未合并候选的 Issue 不是新工作**，不论有没有人记得标签。而标签仍是必需的第二个信号——它是候选关闭或合并后唯一存活的那个。
- `check.py` 对未完成建立的提示不再指向生成结果里并不存在的 `establish.md`，改为给出可执行动作。

依据来自两次零上下文外部审查（改前读 11 份文档未能开工；改后 1 份文档约 1.3 万字符即可判断），见[决策记录 0013](docs/decisions/0013-preconditions-and-takeability.md)。

## 0.1.0a7 — 现场生命周期与回收策略

**对采纳者的影响：两条规则 + 一处模板变化。** 机制层新增「收尾责任按可见范围划分」与「禁止绕过同分支检出限制」；入口模板新增「本项目的现场回收策略」一节，影响每一次新安装。已运行 `0.1.0a6` 的项目升级时取新版 `mechanics.md` 即可获得前两条，回收策略需项目自行声明。

**同时确立一条采纳约定**：下游 pin 已发布的版本，不 pin 上游默认分支上的中间状态。上游默认分支始终含未发布改动，pin 它意味着采纳一个作者可能仍在修改的状态。这条约定对上游同样是义务——**已完成的改进只有发布之后才能到达采纳者**。方法见 [migration.md](skills/cybercorp/references/migration.md)。


- **收尾责任按可见范围划分**：创建现场者结束本机现场，**实际集成者结束共享分支与 PR**。留下已合并分支的集成者没有完成集成。此前只说"收尾属于创建该现场的工作"，共享侧无人负责——本仓库七条分支全部已在默认分支而未回收，即此缺口的后果。
- **明令不得用 `--force` 或 `--ignore-other-worktrees` 绕过同分支检出限制**，绕过它会产生占用机制正要防止的冲突写入。
- **入口新增「本项目的现场回收策略」一节**：交付分支在其 tip 成为默认分支祖先后回收；核验对着真实远端做，只操作精确 ref；未合入分支不在策略内。模板侧要求项目自行声明并说明不声明的后果。

理由、被否决的内容（四步核验的具体命令序列不进机制层）与对下游的影响见[决策记录 0010](docs/decisions/0010-site-lifecycle-gaps.md)。

## 0.1.0a6 — 机制层重构与首次真实采纳

**对已运行上一版布局的项目是破坏性的。** 五处需要采纳者动手：`repo-context.py` 输出中 `verified` 改名为 `declared_active`、`atomic_snapshot` 改名为 `paginated_reads`（**语义反转**，`true` 现在表示发生过分页读取）、`check.py` 新增标题锚点校验（此前通过的项目可能开始报错）、`S1`–`S8` 代号与对应节标题取消（旧锚点全部失效）、占用要求一条带实例 ID 的评论（仅 self-assign 不再构成完整占用）。换代方法见 [migration.md](skills/cybercorp/references/migration.md)，逐项差异见本节以下内容。


### 收尾：结果、占用、执行现场分开结束

补齐 [Issue #7](https://github.com/OKFin33/CyberCorp/issues/7) 收缩后的剩余范围。推导、被否决的替代方案与合成消费发现的两处留白见[决策记录 0009](docs/decisions/0009-closeout-is-three-endings.md)。

- **机制层新增一节「结果、占用与执行现场分开结束」**（子问题 7 → 8）。三种结束各有自己的证据与剩余动作，节内以"它不证明什么"一列表达可分辨性：结果的完成不许可删任何本地对象；release 不证明进程停止或候选可弃；目录删掉也不等于现场结束。核验（谁在占用、哪些提交只在这里、哪个共享成果取代了它、还有什么在读它）之后操作单个对象并读回。接班默认延续既有分支与 PR，交回的候选保持开启且可发现；隔离时在两侧记录替代关系与旧成果去向；保留物须写明用途或接续条件。留白带默认行为：项目未声明可弃范围时，回收本次执行创建且独有内容已可从共享成果到达的现场，其余保留并写明理由。
- **入口路由表新增一行**指向该节；`work-corp`、`review-corp`、`prepare-corp` 三份方法的收尾步骤改为路由到它，不复述。
- **四条不得简化的判据以原生 Git 反例固定**（`tests/test_closeout.py`）：`clean -fd` 保留 ignored 内容而 `-fdx` 会删掉项目故意不跟踪的本地数据；merge 之后源分支的新提交不再被包含；squash 之后内容相同而祖先关系不存在；`push --delete` 返回 0 时 ref 消失而对象仍在——退出码描述命令，读回描述状态。
- **`enter.py` 说清它到底做了什么（破坏性：输出字段与 `--dry-run` 语义变化）。** 它创建的是同一克隆的附加 worktree：原工作树、索引与未跟踪内容不变，原库多出取得的提交与新分支引用，结束该现场须回到原库 `git worktree remove`，`rm -rf` 只会留下 prunable 条目。新增 `continues`（开的是新分支，不延续任何既有分支或 PR）与 `closeout` 字段，修正创建后的 `ceiling`；`--dry-run` 不带目标由静默忽略改为报错。CLI 参数未扩张。
- 测试 143 → 150 项。

### 机制层按执行顺序重排

节序原为推导顺序；去掉代号只去掉了表层，排列才是底层。理由、被否决的合并与判据见[决策记录 0007](docs/decisions/0007-restructure-by-execution-order.md)。

- **节序改为一次执行遇到它们的顺序**：找到工作 → 占用 → 单元验收 → 单元大小 → 完成 → 何时必须问 Owner → 事实在哪。
- **占用与接管合并为一节。** 分开时有一处交叉引用作胶带、一条规则说两遍、两节都列同一个原生载体。合并后 4886 字符 9 条故障形态（原 5418 字符 13 条）。
- **Owner 交互在一处闭合。** 请求决定与采纳答复原先分在两节，现在归属信息、适用范围与「采纳或明确拒绝并记录理由」的生命周期与发出请求的边界规则在一起。
- **元规则独立成段并声明不是子问题。** 改这套规则本身不是项目工作，混在持久事实节里让最厚的一节更厚。本轮两条判据随之写入：留白必须带默认行为；每条"消除了哪个猜测"要在最不利的合法状态下检验。
- **新增规则：本目录之外指向规则的任何东西都经入口或路由表**，不直连方法文件。入口与路由表稳定，其背后的文件会被重组；直连的指针在下次重组时静默失效。
- **`check.py` 覆盖锚点校验（破坏性：此前通过的项目可能开始报错）。** 本次改节标题即撞到——入口锚点失效而检查仍报 passed，因为路径还在。
- 节标题变化使旧锚点失效，按固定版本引用 `mechanics.md#…` 的下游需更新。

### 首次真实采纳后的修正

- **去掉 `S1`–`S8` 代号，规则以自己的名字被引用。** 代号源自设计阶段的子问题编号，只记录作者的推导顺序：读者见到 `S7` 必须跳转才知道所指。小标题、交叉引用、三份方法文件与贡献指引中的引用形式全部改为规则名；多数引用因步骤本身已表达规则而直接删除。已发布的决策记录保留原有代号，它们是历史。

首个在另一个真实项目上采纳本机制层的独立实例，其交付经独立只读审查暴露两处上游缺陷。理由、被否决的替代方案与合成消费为何未能发现见[决策记录 0004](docs/decisions/0004-fixes-from-first-real-adoption.md)；本批四项改动共同的缺陷形态——某处声明的比它实际做到的多——见[决策记录 0005](docs/decisions/0005-claims-match-facts.md)。

- **「接管僵死占用」的条件改为可执行的观察记录。** 原文以"超过阈值未变化"为条件却不设默认值，使该节自称消除的猜测在项目未声明阈值时依然存在。现改为：核对最后变化时间、未关闭的 PR 与剩余工作，把观察记录在对象上，然后自我指派；项目可声明阈值作为跳过该记录的捷径。该改动依赖该节原有的"原持有者回来后必须停止"义务来限制过早接管的代价。
- **恢复旧机制中被误删的实例标识与仲裁规则。** 每次执行一个新 ID（附 `uuid4` 生成命令），压缩只在同一次执行内保留它，ID 只标识不授权；发布占用后重读完整评论，确认服务器顺序中第一个有效占用是自己的，败者停止冲突写入。换代时这两条随 `claim-protocol.md` 一并删除，但它们不依赖被删的 lease 与事件流，只依赖评论。理由与判据见[决策记录 0006](docs/decisions/0006-restore-instance-identity.md)。
- **占用改为「自我指派 + 带 instance ID 的评论」。** `assignee` 与 `requested_reviewers` 都是账号级的，无法区分同账号的另一实例或自己上一次运行——而该规则声明要解决的场景就包括"一个实例和它重启后的后继者取同一份工作"。审查路径此前已要求 instance ID，占用路径没有；现在两条统一，并要求占用前先读已有的占用评论。审查方法文件中对该规则的复述同时删除。
- **`atomic_snapshot` 更名为 `paginated_reads`（破坏性，语义反转）。** 报告由七八次顺序读取拼装，从来不是某一瞬间的快照；旧名声称的强于它实际计算的"有没有翻页"。
- **新增模板一致性检查**（`tests/test_template_sync.py`）。本仓库自用的 `.agents/`、`docs/corp/` 与分发模板必须逐字一致，此前该不变量只靠人工比对维持，本次换代中已实际漂移。检查同时区分两类合法差异：入口由安装器替换 token，Owner 卡是项目自有内容而结构须相同。
- 贡献指引新增两条准入判据：留白必须带默认行为；不要写自己检验不了的声明。
- **`repo-context.py` 输出中的 `verified` 更名为 `declared_active`（破坏性）。** 该字段从未读取 target，只反映路由表写了 `active`；旧名称在一个主张"完成声明必须有直接证据"的产品里夸大证据。同时删除无调用者的 `canon_routes(root)`。
- **背景 Milestone 读取失败不再阻断明确请求的 Issue**，改为记入 `errors`；新增 `InconsistentObservation`，使重复读取不一致仍然中止整份观察——对象读不到可以降级，观察方法失效不能。
- 测试由 135 项增至 137 项。

### 机制层收敛为两份文档

- 机制层由 13 份场景文档收敛为两份：[入口](docs/corp/README.md)持有统领规则、开工必读与场景路由，[mechanics.md](docs/corp/mechanics.md) 是全部规则的唯一定义处，按八个子问题组织。每条规则须能指出它服务的子问题与它消除的猜测。
- 引入统领规则：不可逆动作清单由项目声明，清单之外的动作执行者自主，且要求 Owner 介入的一方承担举证责任。此前"普通工程判断不自动上交人工"是与其他需求并列的一条，现在成为其他机制的授权判据。
- 开工前必须遵守的规则从散布于多份文档收敛为入口中的五条。
- 占用机制覆盖到没有预先载体的动作：生成工作与审查各自需要可见占用，规划先创建载体 Issue。
- 认领与释放改用 GitHub 维护的字段（`assignee`、`updated_at`），移除自建的 lease／renewal 记录格式——一次字段笔误曾使整条执行记录不可重放。
- 阶段审查的开启条件改为机器可核对：规划者声明阶段的关闭条件，不再由任何实例判断阶段是否接近完成。
- 事实层的 `unknown` 区分"未决"与"未登记"。
- 仓库改为常规开源结构：新增出处声明、变更记录、贡献指引、决策记录与验证状态；`docs/plan.md`、`docs/corp/preparation.md`、`docs/source-notes.md` 的内容迁入上述位置。
- 移除 `docs/evidence/` 下的 Git bundle、压缩 API 记录与原生状态快照（27 个文件、约 915KB）；证据以报告、固定提交号与 SHA-256 清单形式保留。`docs/evidence/` 由 1065KB 降至 150KB，仓库 tracked 总量由 1.4MB 降至 0.51MB。理由见[决策记录 0002](docs/decisions/0002-remove-verification-artifacts.md)。
- **切断外部代码继承链。** `spec-checkpoint.py` 与 `repo-context.py` 由未接触旧实现的独立实例按功能规格从零重写（108→176 行、447→313 行），接口契约保持不变——Spec 标记、归一化规则与输出格式一致，已发布的 acceptance 哈希不受影响。`work-state.py` 与 `format-event.py` 直接删除，它们是旧"评论即状态机"机制的实现，新机制改用 `assignee` 与 `updated_at` 后不再需要。`tests/protocols/` 从零重写（72 项）。`NOTICE` 删除——出处声明的对象已不存在。理由与隔离条件见[决策记录 0003](docs/decisions/0003-remove-inherited-code.md)。

### 升级须知（不是变更，是迁移辅助）

版本特定信息，换代方法本身见 [migration.md](skills/cybercorp/references/migration.md)。

持有 2026-09-12 之前布局（`docs/corp/` 下十一份方法文档）的项目，删除它们并保留自己的 Owner 卡；规则的新位置：

| 退役文档 | 规则现在的位置 |
|---|---|
| `spec-authoring.md`、`spec-protocol.md` | 工作单元自含验收 |
| `claim-protocol.md` | 占用与接管 |
| `coordination.md` | 总有下一件事（规划载体的收敛）与占用（规划先建载体） |
| `development-loop.md` | 总有下一件事，顺序见 `work-corp/SKILL.md` |
| `verification.md`、`milestone-delivery.md` | 完成以非自证的证据为准 |
| `communication.md` | 不可逆动作的边界——请求与答复现在同属一节 |
| `bootstrap.md` | 入口 `README.md` 与 `prepare-corp/SKILL.md` |
| `preparation.md` | `prepare-corp/SKILL.md` |
| `damaged-history.md` | **无，且是有意的。** 它恢复的是可能变得不可重放的自维护事件流；该事件流已删除，占用改用 GitHub 维护的字段，竞争占用由服务器顺序裁定而非重放。 |

另有两处是行为变化而非文件搬迁：阶段审查不再由"普通 PR 默认触发"开启，改为规划声明的机器可核对关闭条件成立时开启（已接受的明确审查承诺继续有效）；`lease_until`、`renew` 与事件状态机取消，`work-state.py` 与 `format-event.py` 已删除，项目中调用它们的接线需一并移除。

持有 2026-09-12 布局的项目，除本文件其余各节列出的破坏性变更外，注意 `S1`–`S8` 代号已取消、节标题改变导致 `mechanics.md#…` 锚点全部失效。

## 0.1.0a5 — 项目准备与检查反馈

由 [Issue #5](https://github.com/OKFin33/CyberCorp/issues/5) 持有。

- 准备方法引导按项目实际从目标与验收推导粗路线、近期重点、开发骨架与首批工作。
- 按交付缺口选工接入工作循环：队列非空但必要结果缺少承接时仍补齐。
- 新增可携带的本地检查，适配项目实际检查与 CI，记录真实 Git 范围、版本与运行结果。
- 旧项目重装保护与显式采纳已本地核验；独立候选审查发现的阶段路由漏检已修正并复验。

证据：合成消费验证，报告已于 2026-09-13 移除（见决策记录 0008），可从 Git 历史取回。

## 0.1.0a4 — 阶段交付与 Owner 沟通

由 [Issue #3](https://github.com/OKFin33/CyberCorp/issues/3) 与 [PR #4](https://github.com/OKFin33/CyberCorp/pull/4) 持有，已合入 `7ebc4a6`。

- 阶段候选、修正复验与交付方法。
- Owner 沟通卡机制：表达偏好可替换，不改变事实、验收与权限。

证据：合成行为验证，报告与 bundle 均已移除（见决策记录 0002、0008），可从 Git 历史取回。

## 初始 — 自身协作与基本准备

由 [Issue #1](https://github.com/OKFin33/CyberCorp/issues/1) 持有。

- 自包含创建包：确定性安装、项目自有的准备／工作／审查方法、原生 Spec、占用与上下文工具。
- 独立 checkout 入口保留原 clone 及其未提交工作；方法更新不自动改任务 pin。
- 可选启动器独立携带，支持显式 CLI 命令与用户自有适配器，Kiro 是其中一个示例。
